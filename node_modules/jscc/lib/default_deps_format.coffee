exports.DEFAULT_DEPS_FORMAT = """(function (deps) {
        for (var i = 0, len = deps.length; i < len; i++)
                document.write('<script type=\"text/javascript\" src=\"' + deps[i] + '\"><' + '/script>');
        })(${DEPS_LIST})\n"""