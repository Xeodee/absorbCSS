growl = require 'growl'
require "colors"
exports.log = (message, title) ->
        showTitle = if title then title else 'compressor notification.'
        growl message, {title : showTitle}
        console.log "#{showTitle.yellow} : #{message}"

        